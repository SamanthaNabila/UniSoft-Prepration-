# Notes CLI

Milestone 1 provides the command-line skeleton for the notes project.

```text
python notes_cli.py add
python notes_cli.py list
python notes_cli.py search
python notes_cli.py delete
```

Each command currently prints `not implemented`.
